Reservado. Ainda não implementado nesta etapa.

Ao construir, reusar os tokens de `styles/tokens.css` e os componentes de
`components/product/` — esta rota deve parecer parte do mesmo sistema que
`/vision` e `/anchor`.
